{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "JudgeResult",
  "type": "object",
  "additionalProperties": false,
  "required": ["case_id", "dimension", "evaluation_job_id", "skill_sha256", "human_image_sha256", "model_image_sha256", "comparison_reasoning", "winner"],
  "properties": {
    "case_id": {"type": "string", "pattern": "^case_[0-9a-f]{16}$"},
    "dimension": {
      "enum": ["faithfulness", "conciseness", "readability", "aesthetics"]
    },
    "evaluation_job_id": {"type": "string", "pattern": "^[0-9a-f]{24}$"},
    "skill_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "human_image_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "model_image_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "comparison_reasoning": {"type": "string", "minLength": 1},
    "winner": {
      "enum": ["Human", "Model", "Both are good", "Both are bad"]
    }
  }
}
